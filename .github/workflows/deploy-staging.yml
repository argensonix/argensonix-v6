name: Deploy Staging (Raspberry Pi)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, staging, rpi5, org-staging]

    steps:
      - name: Deploy staging
        run: |
          bash /srv/repos/personal/argensonix/argensonix-v6/scripts/deploy-staging.sh
